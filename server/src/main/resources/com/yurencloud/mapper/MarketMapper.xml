<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yurencloud.mapper.MarketMapper">
  <sql id="add">

  </sql>
  <insert id="insertKline" parameterType="lang.String">
    INSERT INTO market_kline(kline) VALUES(#{kline,jdbcType=VARCHAR})
  </insert>
</mapper>