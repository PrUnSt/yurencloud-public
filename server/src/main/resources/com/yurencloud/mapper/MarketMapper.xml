<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yurencloud.mapper.MarketMapper">
  <sql id="add">

  </sql>
  <insert id="insertKline" parameterType="com.yurencloud.model.MarketKline">
    INSERT INTO market_kline(time, kline) VALUES(#{time}, #{kline})
  </insert>
  <insert id="insertHistoryKline" parameterType="com.yurencloud.model.MarketKline">
    INSERT INTO market_kline(time, kline) VALUES(#{time}, #{kline})
  </insert>
</mapper>